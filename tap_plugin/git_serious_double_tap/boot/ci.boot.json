{
  "version": 1,
  "description": "git_serious_double_tap CI record (req-boot-bootstrap-ci-record, tap#365) — the stack this plugin's own tests run in, and the one claim per-plugin CI is allowed to make: that git_serious_double_tap is correct against the dependencies it DECLARES and nothing else. Install set is exactly the depends_on closure (identity_core, git_core, compliance_core, administrivia, github_core, git_serious) plus git_serious_double_tap itself; the consumer flips the self entry to editable so the CHECKOUT under test runs, not the pinned revision. Population seeds git_serious, git_serious_double_tap (seed-only, offline). DELIBERATELY EXCLUDED: every other plugin, all credentials, all required_secrets, and any population step that reaches the network — so a fork can run this lane and a nightly cannot flake on a third party (req-boot-bootstrap-ci-record-5). Reproduce CI exactly: spawn --from 'git+https://github.com/unified-systems-com/git-serious-double-tap@03f871a3c5a84310f7a2cb461a90dc8017cb2d05#ci' --dev-plugins git_serious_double_tap",
  "install": {
    "plugins": [
      {
        "slug": "identity_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-identity-core",
          "rev": "v0.1.2"
        },
        "note": "Transitive: a dependency of git_serious. Pinned at v0.1.2, the release current on 2026-09-09. Public repo, no credential (req-tap-plugin-arch-source-secret-5)."
      },
      {
        "slug": "git_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/git-core-tap",
          "rev": "v0.1.0"
        },
        "note": "Transitive: a dependency of git_serious. Pinned at v0.1.0, the release current on 2026-09-09. Public repo, no credential (req-tap-plugin-arch-source-secret-5)."
      },
      {
        "slug": "compliance_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-compliance-core",
          "rev": "v0.3.0"
        },
        "note": "Transitive: a dependency of git_serious. Pinned at v0.2.2, the release current on 2026-09-09. Public repo, no credential (req-tap-plugin-arch-source-secret-5)."
      },
      {
        "slug": "administrivia",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-administrivia",
          "rev": "v0.1.1"
        },
        "note": "Transitive: a dependency of git_serious. Pinned at v0.1.1, the release current on 2026-09-09. Public repo, no credential (req-tap-plugin-arch-source-secret-5)."
      },
      {
        "slug": "github_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-github-core",
          "rev": "v0.10.0"
        },
        "note": "Transitive: a dependency of git_serious. Pinned at v0.6.0, the release current on 2026-09-09. Public repo, no credential (req-tap-plugin-arch-source-secret-5). Moved to v0.10.0 on 2026-09-11: settings into configuration (#110) and secrets nested in the machinery picture (#116), which this instance's lanes layout restates."
      },
      {
        "slug": "zizmor",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/zizmor-tap",
          "rev": "main"
        },
        "note": "Offline GitHub Actions workflow audits. In the ci closure because this plugin's instance record seeds and fires it, so the slug must resolve here too."
      },
      {
        "slug": "git_serious",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/git-serious-tap",
          "rev": "e8af7bcc11ada7ddfc1a61e94e081ae6524c1952"
        },
        "note": "Declared in depends_on. Pinned at 8e390633f5db3ccd2f17a646813a3285cc30cf5a, the release current on 2026-09-09. Public repo, no credential (req-tap-plugin-arch-source-secret-5)."
      },
      {
        "slug": "git_serious_double_tap",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/git-serious-double-tap",
          "rev": "03f871a3c5a84310f7a2cb461a90dc8017cb2d05"
        },
        "note": "Self, pinned like any app-of-apps record — the CONSUMER (plugin-ci boot-and-test, or `spawn --dev-plugins git_serious_double_tap`) substitutes the checkout under test via tap.dev_workspace (req-boot-bootstrap-ci-record-4). Booting this record unflipped runs the pinned revision."
      }
    ]
  },
  "population": {
    "on_failure": "abort",
    "steps": [
      {
        "type": "seed-plugin",
        "plugin": "git_serious",
        "enabled": true,
        "note": "git_serious's GRIFT, which this plugin's pages reference or link to."
      },
      {
        "type": "seed-plugin",
        "plugin": "git_serious_double_tap",
        "enabled": true,
        "note": "Own GRIFT: seeding it proves the bundle parses and its panel types resolve against a real registry."
      }
    ]
  },
  "fips_waivers": [
    {
      "artifact": "*/bin/zizmor",
      "provider": "boringssl",
      "reason": "zizmor's wheel is one static Rust binary that links boringssl (via ring) and aws-lc-rs for an HTTPS client used only by its ONLINE audits. This plugin invokes it with --offline and hands it an environment carrying no GitHub token, so the providers are present in the artifact but never execute a security operation. No TAP data is encrypted, signed or hashed by them. Re-decide when req-zizmor-online-audits ships: that turns the providers from present-but-unreached into actually executing. (Scoped by `artifact` rather than `plugin`: tap.crypto_bom.load_waivers resolves `entry.get('artifact') or entry.get('plugin')`, so supplying both would silently drop the plugin key — a field that reads as if it narrows the waiver and does not.)"
    },
    {
      "artifact": "*/bin/zizmor",
      "provider": "rust-aws-lc-rs",
      "reason": "The second non-validated provider in the same static binary, waived on the same basis and with the same re-decision trigger. AWS-LC has a FIPS 140-3 validated module, but it ships as a separate crate (aws-lc-fips-sys) that zizmor's build does not select, so the binary is not the validated module even though the library it embeds is validatable. (Scoped by `artifact` rather than `plugin`: tap.crypto_bom.load_waivers resolves `entry.get('artifact') or entry.get('plugin')`, so supplying both would silently drop the plugin key — a field that reads as if it narrows the waiver and does not.)"
    }
  ]
}
