"""git-serious double-tap — git-serious pointed at the organization that builds TAP.

A *double-tap* plugin is one built to introspect the TAP system and platform itself. This one
holds only what is specific to the unified-systems-com instance of git-serious: the pages and
panels needed to work a running example of our own CI/CD system. It ships no models, edges or
collectors; every repeatable capability it discovers is pushed down into git_serious or
github_core, and what remains here is instance configuration.
"""
