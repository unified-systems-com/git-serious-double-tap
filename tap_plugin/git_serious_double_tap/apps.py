"""git-serious double-tap plugin AppConfig — configuration-only; the base ready() registers the manifest."""

from tap_plugins.base import TapPluginConfig


class GitSeriousDoubleTapConfig(TapPluginConfig):
    pass
